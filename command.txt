hexo clean && hexo g && hexo s
hexo clean && hexo g && hexo d

 set http_proxy=http://127.0.0.1:7890
set https_proxy=http://127.0.0.1:7890
hexo g && hexo d

hexo g && hexo s
hexo g && hexo d

hexo new post "Debug" # 会在 source/_posts/ 目录下生成文件 ‘test.md’，打开编辑
hexo generate        # 生成静 态HTML文件到 /public 文件夹中
hexo server          # 本地运行server服务预览，打开 http://localhost:4000 即可预览你的博客

hexo new post "【Fufu建模与动画2】前期准备"
hexo new page 